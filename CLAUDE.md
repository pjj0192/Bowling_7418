# Bowling

## 작업 방식

이 프로젝트는 TDD(Test-Driven Development) 기반으로 볼링 점수 계산 로직을 작성한다.

- 항상 실패하는 테스트를 먼저 작성한 뒤, 그 테스트를 통과시키는 최소한의 코드를 작성한다 (Red -> Green -> Refactor).
- 한 번에 여러 기능을 구현하지 않고, 작은 단위의 테스트-구현 사이클을 반복한다.
- 구체적인 규칙, 코딩 컨벤션, 진행 방식은 `tdd-skill` 스킬(`.claude/skills/tdd-skill/SKILL.md`)을 따른다.
